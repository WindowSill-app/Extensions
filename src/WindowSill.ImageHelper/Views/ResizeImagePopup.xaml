<?xml version="1.0" encoding="utf-8" ?>
<api:SillPopupContent
    x:Class="WindowSill.ImageHelper.Views.ResizeImagePopup"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:api="using:WindowSill.API"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Width="350"
    Height="275"
    Opening="SillPopupContent_Opening"
    mc:Ignorable="d">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!--  Tab Selector  -->
        <SelectorBar
            x:Name="ResizeModeSelectorBar"
            Grid.Row="0"
            Margin="24,8,24,8"
            SelectionChanged="SelectorBar_SelectionChanged">
            <SelectorBarItem
                x:Name="AbsoluteSizeBarItem"
                api:Uids.Uid="/WindowSill.ImageHelper/ResizeImage/AbsoluteSizeBarItem"
                IsSelected="True" />
            <SelectorBarItem x:Name="PercentageBarItem" api:Uids.Uid="/WindowSill.ImageHelper/ResizeImage/PercentageBarItem" />
        </SelectorBar>

        <!--  Content Frame  -->
        <Frame
            x:Name="ContentFrame"
            Grid.Row="1"
            Padding="24,0,24,0"
            HorizontalAlignment="Stretch"
            VerticalAlignment="Stretch"
            HorizontalContentAlignment="Stretch"
            VerticalContentAlignment="Stretch"
            IsNavigationStackEnabled="False">
            <Frame.ContentTransitions>
                <TransitionCollection>
                    <NavigationThemeTransition>
                        <NavigationThemeTransition.DefaultNavigationTransitionInfo>
                            <SlideNavigationTransitionInfo Effect="FromRight" />
                        </NavigationThemeTransition.DefaultNavigationTransitionInfo>
                    </NavigationThemeTransition>
                </TransitionCollection>
            </Frame.ContentTransitions>
        </Frame>

        <!--  Bottom Command Bar  -->
        <Border
            Grid.Row="2"
            Padding="24"
            HorizontalAlignment="Stretch"
            VerticalAlignment="Bottom"
            Background="{ThemeResource LayerOnAcrylicFillColorDefaultBrush}"
            BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
            BorderThickness="0,1,0,0">
            <Grid ColumnSpacing="8">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>

                <ProgressRing HorizontalAlignment="Right" IsIndeterminate="{x:Bind ViewModel.IsResizeInProgress, Mode=OneWay}" />

                <Button
                    Grid.Column="1"
                    api:Uids.Uid="/WindowSill.ImageHelper/ResizeImage/ResizeButton"
                    Command="{x:Bind ViewModel.ResizeCommand}"
                    IsEnabled="{x:Bind ViewModel.IsResizeInProgress, Mode=OneWay, Converter={StaticResource InverseBoolConverter}}"
                    Style="{StaticResource LargeButtonStyle}" />
            </Grid>
        </Border>
    </Grid>
</api:SillPopupContent>
