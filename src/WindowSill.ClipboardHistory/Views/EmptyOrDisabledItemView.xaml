<?xml version="1.0" encoding="utf-8" ?>
<UserControl
    x:Class="WindowSill.ClipboardHistory.Views.EmptyOrDisabledItemView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:api="using:WindowSill.API"
    xmlns:converters="using:CommunityToolkit.WinUI.Converters"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="ms-appx:///WindowSill.API/Themes/Generic.xaml" />
            </ResourceDictionary.MergedDictionaries>

            <converters:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter" />
            <converters:BoolToVisibilityConverter
                x:Key="InverseBoolToVisibilityConverter"
                FalseValue="Visible"
                TrueValue="Collapsed" />
        </ResourceDictionary>
    </UserControl.Resources>

    <api:SillOrientedStackPanel Spacing="8">
        <Image
            Width="{ThemeResource SillIconSize}"
            Height="{ThemeResource SillIconSize}"
            HorizontalAlignment="Center"
            VerticalAlignment="Center"
            Source="{x:Bind ViewModel.PluginIconUri}" />

        <Grid>
            <StackPanel Spacing="8" Visibility="{x:Bind ViewModel.IsClipboardHistoryEnabled, Mode=OneWay, Converter={StaticResource InverseBoolToVisibilityConverter}}">
                <HyperlinkButton
                    HorizontalAlignment="Stretch"
                    VerticalAlignment="Stretch"
                    HorizontalContentAlignment="Center"
                    Command="{x:Bind ViewModel.OpenWindowsClipboardHistoryCommand}">
                    <TextBlock
                        HorizontalAlignment="Center"
                        VerticalAlignment="Center"
                        api:Uids.Uid="/WindowSill.ClipboardHistory/Misc/EnableClipboardHistory"
                        TextAlignment="Center"
                        TextWrapping="WrapWholeWords" />
                </HyperlinkButton>
            </StackPanel>

            <TextBlock
                HorizontalAlignment="Center"
                VerticalAlignment="Center"
                api:Uids.Uid="/WindowSill.ClipboardHistory/Misc/EmptyClipboard"
                Style="{ThemeResource CaptionTextBlockStyle}"
                TextAlignment="Center"
                TextWrapping="WrapWholeWords"
                Visibility="{x:Bind ViewModel.IsClipboardHistoryEnabled, Mode=OneWay, Converter={StaticResource BoolToVisibilityConverter}}" />
        </Grid>
    </api:SillOrientedStackPanel>
</UserControl>
