<?xml version="1.0" encoding="utf-8" ?>
<UserControl
    x:Class="WindowSill.AppLauncher.Settings.AddUrlFlyout"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:api="using:WindowSill.API"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid
        Width="300"
        RowSpacing="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock
            Grid.Row="0"
            api:Uids.Uid="/WindowSill.AppLauncher/AddUrlFlyout/Title"
            Style="{ThemeResource SubtitleTextBlockStyle}"
            TextWrapping="WrapWholeWords" />

        <TextBox
            x:Name="UrlTextBox"
            Grid.Row="1"
            api:Uids.Uid="/WindowSill.AppLauncher/AddUrlFlyout/Url"
            TextChanged="UrlTextBox_TextChanged" />

        <TextBox
            x:Name="DisplayNameTextBox"
            Grid.Row="2"
            api:Uids.Uid="/WindowSill.AppLauncher/AddUrlFlyout/DisplayName" />

        <Button
            x:Name="AddButton"
            Grid.Row="3"
            HorizontalAlignment="Right"
            api:Uids.Uid="/WindowSill.AppLauncher/AddUrlFlyout/Add"
            Click="AddButton_Click"
            IsEnabled="False"
            Style="{ThemeResource AccentButtonStyle}" />
    </Grid>
</UserControl>
