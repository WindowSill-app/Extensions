<?xml version="1.0" encoding="utf-8" ?>
<UserControl
    x:Class="WindowSill.Date.FirstTimeSetup.AccountsSetupContributorView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:api="using:WindowSill.API"
    xmlns:ctk="using:CommunityToolkit.WinUI.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:ui="using:CommunityToolkit.WinUI"
    xmlns:vm="using:WindowSill.Date.ViewModels"
    mc:Ignorable="d">

    <StackPanel Spacing="16">
        <TextBlock api:Uids.Uid="/WindowSill.Date/FirstTimeSetup/AccountsDescription" TextWrapping="WrapWholeWords" />

        <!--  Add account button  -->
        <DropDownButton>
            <StackPanel Orientation="Horizontal" Spacing="8">
                <FontIcon FontSize="14" Glyph="&#xE710;" />
                <TextBlock api:Uids.Uid="/WindowSill.Date/FirstTimeSetup/AddAccount" />
            </StackPanel>
            <DropDownButton.Flyout>
                <MenuFlyout x:Name="AddAccountMenuFlyout" Placement="Bottom" />
            </DropDownButton.Flyout>
        </DropDownButton>

        <!--  Empty state  -->
        <TextBlock
            x:Name="NoAccountsText"
            api:Uids.Uid="/WindowSill.Date/FirstTimeSetup/NoAccountsMessage"
            Foreground="{ThemeResource TextFillColorSecondaryBrush}"
            Style="{StaticResource CaptionTextBlockStyle}" />

        <!--  Account list  -->
        <ItemsRepeater x:Name="AccountsRepeater">
            <ItemsRepeater.Layout>
                <StackLayout Spacing="2" />
            </ItemsRepeater.Layout>
            <ItemsRepeater.ItemTemplate>
                <DataTemplate x:DataType="vm:AccountViewModel">
                    <ctk:SettingsCard Description="{x:Bind Email}" Header="{x:Bind DisplayName}">
                        <ctk:SettingsCard.HeaderIcon>
                            <ImageIcon Source="{x:Bind ProviderIconSource}" />
                        </ctk:SettingsCard.HeaderIcon>
                        <Button
                            Width="32"
                            Height="32"
                            Padding="0"
                            VerticalAlignment="Center"
                            api:Uids.Uid="/WindowSill.Date/FirstTimeSetup/RemoveAccount"
                            Background="Transparent"
                            BorderBrush="Transparent"
                            Command="{x:Bind RemoveCommand}"
                            Content="{ui:FontIcon FontFamily={StaticResource SegoeFluentIcons},
                                                  Glyph=&#xE74D;,
                                                  FontSize=16}" />
                    </ctk:SettingsCard>
                </DataTemplate>
            </ItemsRepeater.ItemTemplate>
        </ItemsRepeater>
    </StackPanel>
</UserControl>
