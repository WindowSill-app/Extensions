<?xml version="1.0" encoding="utf-8" ?>
<UserControl
    x:Class="WindowSill.Date.Views.MeetingNotificationContent"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:api="using:WindowSill.API"
    xmlns:converters="using:CommunityToolkit.WinUI.Converters"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <UserControl.Resources>
        <converters:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter" />
    </UserControl.Resources>

    <Grid>
        <MediaPlayerElement
            x:Name="BackgroundMediaPlayer"
            Width="0"
            Height="0"
            HorizontalAlignment="Left"
            VerticalAlignment="Top"
            AreTransportControlsEnabled="False"
            IsFullWindow="False"
            Loaded="BackgroundMediaPlayer_Loaded"
            Stretch="None" />

        <Border
            MaxWidth="1200"
            Margin="72"
            HorizontalAlignment="Center"
            VerticalAlignment="Center"
            Background="{ThemeResource LayerOnAcrylicFillColorDefaultBrush}"
            BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
            BorderThickness="1"
            CornerRadius="{ThemeResource OverlayCornerRadius}">

            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="*" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <!--  Meeting content  -->
                <StackPanel
                    Grid.Row="0"
                    Padding="36,40,36,40"
                    Background="{ThemeResource LayerOnAcrylicFillColorDefaultBrush}"
                    Spacing="16">
                    <StackPanel.ChildrenTransitions>
                        <EntranceThemeTransition IsStaggeringEnabled="True" />
                    </StackPanel.ChildrenTransitions>

                    <!--  "Meeting is starting" header  -->
                    <TextBlock
                        HorizontalAlignment="Stretch"
                        api:Uids.Uid="/WindowSill.Date/Meetings/NotificationHeader"
                        CharacterSpacing="200"
                        Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                        HorizontalTextAlignment="Center"
                        Style="{ThemeResource BodyTextBlockStyle}" />

                    <!--  Meeting title (large)  -->
                    <TextBlock
                        HorizontalAlignment="Stretch"
                        FontSize="40"
                        FontWeight="SemiBold"
                        MaxLines="3"
                        Text="{x:Bind ViewModel.MeetingTitle, Mode=OneTime}"
                        TextAlignment="Center"
                        TextTrimming="WordEllipsis"
                        TextWrapping="WrapWholeWords" />

                    <!--  Time range  -->
                    <TextBlock
                        HorizontalAlignment="Stretch"
                        FontSize="16"
                        Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                        HorizontalTextAlignment="Center"
                        Text="{x:Bind ViewModel.TimeText, Mode=OneTime}" />
                </StackPanel>

                <!--  Actions footer  -->
                <Border
                    Grid.Row="1"
                    Padding="36,24,36,24"
                    HorizontalAlignment="Stretch"
                    VerticalAlignment="Bottom"
                    BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                    BorderThickness="0,1,0,0">

                    <StackPanel
                        HorizontalAlignment="Center"
                        Orientation="Horizontal"
                        Spacing="8">

                        <!--  Join meeting button (primary, if video call)  -->
                        <Button
                            Command="{x:Bind ViewModel.JoinAndDismissCommand}"
                            Style="{ThemeResource AccentButtonStyle}"
                            Visibility="{x:Bind ViewModel.HasVideoCall, Converter={StaticResource BoolToVisibilityConverter}}">
                            <StackPanel Orientation="Horizontal" Spacing="8">
                                <FontIcon FontSize="16" Glyph="&#xE714;" />
                                <TextBlock Text="{x:Bind ViewModel.JoinButtonText}" />
                            </StackPanel>
                        </Button>

                        <!--  Dismiss button  -->
                        <Button Command="{x:Bind ViewModel.DismissCommand}">
                            <StackPanel Orientation="Horizontal" Spacing="8">
                                <FontIcon FontSize="16" Glyph="&#xE894;" />
                                <TextBlock api:Uids.Uid="/WindowSill.Date/Meetings/NotificationDismiss" />
                            </StackPanel>
                        </Button>
                    </StackPanel>
                </Border>
            </Grid>
        </Border>
    </Grid>
</UserControl>
