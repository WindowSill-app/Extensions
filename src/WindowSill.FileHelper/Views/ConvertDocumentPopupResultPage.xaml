<?xml version="1.0" encoding="utf-8" ?>
<Page
    x:Class="WindowSill.FileHelper.Views.ConvertDocumentPopupResultPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:api="using:WindowSill.API"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:services="using:WindowSill.FileHelper.Services"
    mc:Ignorable="d">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <ScrollViewer
            Grid.Row="0"
            HorizontalScrollMode="Disabled"
            VerticalScrollMode="Auto">
            <StackPanel Margin="24" Spacing="16">

                <!--  Result Summary  -->
                <StackPanel HorizontalAlignment="Center" Spacing="16">
                    <FontIcon
                        x:Name="ResultIcon"
                        HorizontalAlignment="Center"
                        FontSize="48"
                        Foreground="{ThemeResource SystemFillColorSuccessBrush}"
                        Glyph="&#xE73E;" />

                    <TextBlock
                        x:Name="ResultTitle"
                        HorizontalAlignment="Center"
                        AutomationProperties.AutomationId="ConvertDocumentResultTitle"
                        Style="{StaticResource SubtitleTextBlockStyle}" />

                    <TextBlock
                        x:Name="SummaryText"
                        HorizontalAlignment="Center"
                        AutomationProperties.AutomationId="ConvertDocumentResultSummary"
                        Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                        Style="{StaticResource BodyTextBlockStyle}"
                        TextAlignment="Center" />
                </StackPanel>

                <!--
                    Per-file failure details: shown only when at least one file failed, so a failed conversion is
                    always explained instead of only reporting an opaque aggregate count.
                -->
                <ItemsControl x:Name="FailedFilesList" AutomationProperties.AutomationId="ConvertDocumentFailedFilesList">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate x:DataType="services:FileOperationTaskItem">
                            <Border
                                Margin="0,0,0,8"
                                Padding="12,10"
                                Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                                CornerRadius="{ThemeResource ControlCornerRadius}">
                                <StackPanel Spacing="2">
                                    <TextBlock
                                        AutomationProperties.Name="{x:Bind FileName}"
                                        Style="{StaticResource BodyStrongTextBlockStyle}"
                                        Text="{x:Bind FileName}"
                                        TextTrimming="CharacterEllipsis" />
                                    <TextBlock
                                        AutomationProperties.Name="{x:Bind ErrorMessage}"
                                        Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                        Style="{StaticResource CaptionTextBlockStyle}"
                                        Text="{x:Bind ErrorMessage}"
                                        TextWrapping="Wrap" />
                                </StackPanel>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </StackPanel>
        </ScrollViewer>

        <!--  Bottom Bar with Open Folder Button  -->
        <Border
            Grid.Row="1"
            Padding="24"
            HorizontalAlignment="Stretch"
            VerticalAlignment="Bottom"
            Background="{ThemeResource LayerOnAcrylicFillColorDefaultBrush}"
            BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
            BorderThickness="0,1,0,0"
            XYFocusKeyboardNavigation="Enabled">
            <Grid ColumnSpacing="8">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>

                <Button
                    x:Name="OpenFolderButton"
                    Grid.Column="1"
                    api:Uids.Uid="/WindowSill.FileHelper/ConvertDocument/OpenFolderButton"
                    AutomationProperties.AutomationId="ConvertDocumentOpenFolderButton"
                    Command="{x:Bind ViewModel.OpenOutputFolderCommand}"
                    ElementSoundMode="FocusOnly"
                    Style="{ThemeResource LargeAccentButtonStyle}" />
            </Grid>
        </Border>
    </Grid>
</Page>
