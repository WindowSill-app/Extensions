<?xml version="1.0" encoding="utf-8" ?>
<UserControl
    x:Class="WindowSill.UniversalCommands.Settings.SettingsView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:api="using:WindowSill.API"
    xmlns:controls="using:CommunityToolkit.WinUI.Controls"
    xmlns:core="using:WindowSill.UniversalCommands.Core"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    x:Name="Root"
    mc:Ignorable="d">

    <StackPanel Spacing="2">
        <Button
            Margin="0,0,0,8"
            api:Uids.Uid="/WindowSill.UniversalCommands/SettingsView/AddAction"
            Command="{x:Bind ViewModel.AddCommand}">
            <StackPanel Orientation="Horizontal" Spacing="8">
                <FontIcon FontSize="14" Glyph="&#xE710;" />
                <TextBlock api:Uids.Uid="/WindowSill.UniversalCommands/SettingsView/AddActionText" />
            </StackPanel>
        </Button>

        <StackPanel
            HorizontalAlignment="Center"
            VerticalAlignment="Center"
            Spacing="16"
            Visibility="{x:Bind ViewModel.IsEmpty, Converter={StaticResource BoolToVisibilityConverter}, Mode=OneWay}">
            <Image
                x:Name="EmptyStateImage"
                Width="128"
                Height="128" />
            <TextBlock
                MaxWidth="500"
                HorizontalAlignment="Center"
                api:Uids.Uid="/WindowSill.UniversalCommands/SettingsView/EmptyIntro"
                Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                Style="{StaticResource CaptionTextBlockStyle}"
                TextAlignment="Center" />
        </StackPanel>

        <ListView
            AllowDrop="True"
            CanDragItems="True"
            CanReorderItems="True"
            ItemsSource="{x:Bind ViewModel.Commands, Mode=OneWay}"
            SelectionMode="None">
            <ListView.ItemsPanel>
                <ItemsPanelTemplate>
                    <!--  Disable virtualization as it causes crash when reordering items  -->
                    <StackPanel Orientation="Vertical" />
                </ItemsPanelTemplate>
            </ListView.ItemsPanel>
            <ListView.ItemContainerStyle>
                <Style BasedOn="{StaticResource DefaultListViewItemStyle}" TargetType="ListViewItem">
                    <Setter Property="Margin" Value="0" />
                    <Setter Property="Padding" Value="0,2,0,0" />
                </Style>
            </ListView.ItemContainerStyle>
            <ListView.ItemTemplate>
                <DataTemplate x:DataType="core:UniversalCommand">
                    <Border
                        Height="64"
                        VerticalAlignment="Top"
                        Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                        BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                        BorderThickness="1"
                        CornerRadius="{ThemeResource ControlCornerRadius}"
                        PointerEntered="Border_PointerEntered"
                        PointerExited="Border_PointerExited"
                        PointerPressed="Border_PointerPressed">
                        <Grid HorizontalAlignment="Stretch" ColumnSpacing="12">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="32" />
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>

                            <TextBlock
                                Grid.Column="0"
                                HorizontalAlignment="Right"
                                VerticalAlignment="Center"
                                FontFamily="Segoe UI Symbol"
                                FontSize="20"
                                Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                Text="&#x283F;" />

                            <Grid
                                Grid.Column="1"
                                Width="24"
                                Height="24"
                                VerticalAlignment="Center">
                                <FontIcon
                                    FontFamily="{StaticResource SegoeFluentIcons}"
                                    FontSize="16"
                                    Glyph="{x:Bind IconGlyph, Mode=OneWay}" />
                                <Image
                                    Width="24"
                                    Height="24"
                                    Source="{x:Bind IconImagePath, Mode=OneWay}"
                                    Stretch="Uniform" />
                            </Grid>

                            <TextBlock
                                Grid.Column="2"
                                HorizontalAlignment="Stretch"
                                VerticalAlignment="Center"
                                Text="{x:Bind Name}"
                                TextTrimming="CharacterEllipsis" />

                            <StackPanel
                                Grid.Column="3"
                                Margin="0,0,16,0"
                                HorizontalAlignment="Right"
                                Orientation="Horizontal"
                                Spacing="4">
                                <Button
                                    Width="32"
                                    Height="32"
                                    Padding="0"
                                    api:Uids.Uid="/WindowSill.UniversalCommands/SettingsView/EditButton"
                                    Background="Transparent"
                                    BorderBrush="Transparent"
                                    Click="EditButton_Click">
                                    <FontIcon FontSize="12" Glyph="&#xE70F;" />
                                </Button>
                                <Button
                                    Width="32"
                                    Height="32"
                                    Padding="0"
                                    api:Uids.Uid="/WindowSill.UniversalCommands/SettingsView/DeleteButton"
                                    Background="Transparent"
                                    BorderBrush="Transparent"
                                    Click="DeleteButton_Click">
                                    <FontIcon FontSize="12" Glyph="&#xE74D;" />
                                </Button>
                            </StackPanel>
                        </Grid>
                    </Border>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </StackPanel>
</UserControl>
